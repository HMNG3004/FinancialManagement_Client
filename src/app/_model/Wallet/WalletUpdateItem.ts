export interface WalletUpdateItem {
  walletName: string;
  walletBalance: number;
  walletTypeId: number;
  accountCreatedId: number;
  walletNumber: string;
  walletDescription: string;
  walletId: number;
}
